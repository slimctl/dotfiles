## General

- Extremely concise; sacrifice grammar for concision.
- Minimal, happy-path solutions; no over-engineering (dedup pools, helper exports, service layers) unless asked.
- Prefer well-known libraries over custom code.
- Don't run system-mutating commands (installs, applies, uninstalls); show the command and let me run it.

## Superpowers overrides (outrank skill defaults)

- Skip spec-review: summarize spec inline, flag assumptions/open questions, wait for reply before writing plans.
